// 容錯量子計算 - 預留檔案
// 目前原始 data.js 中沒有專屬於容錯分類的節點資料
// 此檔案預留給未來擴充

const toleranceData = {
    // 節點位置配置
    items: [],

    // 節點詳細資料
    details: {
        // 預留給未來的容錯相關節點
        // 例如: Threshold theorem, Magic state distillation, etc.
    },

    // 預留說明
    note: '此檔案為預留結構，原始 data.js 中「容錯」相關節點尚未獨立定義。未來可在此擴充。'
};

// 導出模組
if (typeof module !== 'undefined' && module.exports) {
    module.exports = toleranceData;
}
